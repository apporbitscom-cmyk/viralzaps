/**
 * YouTube Data API v3 configuration
 *
 * 1. Go to https://console.cloud.google.com
 * 2. Create or select a project
 * 3. Enable "YouTube Data API v3" (APIs & Services → Library → search "YouTube Data API v3")
 * 4. Create credentials: APIs & Services → Credentials → Create credentials → API key
 * 5. Restrict the key (optional): set HTTP referrer to your domain for production
 * 6. Replace the value below with your API key
 */
var YOUTUBE_API_KEY = 'YOUR_YOUTUBE_API_KEY';
